---
labels: ['react', 'ui-component', 'text', 'title']
description: 'Subtitle component.'
---

import { Subtitle } from './sub-title';

The subtitle component renders a "muted" extra-large text (as a 'p' element).  
Using the Subtitle component:

```js live
<Subtitle>This is a subtitle</Subtitle>
```
