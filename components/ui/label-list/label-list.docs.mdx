---
labels: ['react', 'ui-component', 'label', 'list']
description: 'Renders an array of labels.'
---

import { LabelList } from './label-list';

The Label List renders an array as labels.

Using the Label List component:

```js live
<LabelList>{['first', 'second', 'third']}</LabelList>
```
