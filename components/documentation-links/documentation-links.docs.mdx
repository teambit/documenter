---
description: The links to our docs pages
labels: ['documentation', 'links']
---

import { links, legacyDocsUrl, docsUrl } from './documentation-links'

This is where we keep all the documentation links

## Usage

```js live
() => {
  return links.scopedRegistry
}
```

We also have a `docsUrl` which is used as the base URL for the docs

```js live
() => {
  return docsUrl
}
```
